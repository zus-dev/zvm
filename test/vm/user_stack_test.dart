import 'package:test/test.dart';

import '../helpers.dart';

/// Tests the access method to the user stack.
/// Note: There is no usable user stack implementation anymore, we have to think
/// about a well-working alternative
void main() {
  test('Dummy', () {
    assertTrue(true);
  });
}
